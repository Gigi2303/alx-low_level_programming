#include <stdio.h>

/**
 * main - this is the main function
 *
 * Return: always 0
 */

int main(void)
{
char str[] = "Programming is like building a multilingual puzzle/n";

puts(str);

return (0);
}
